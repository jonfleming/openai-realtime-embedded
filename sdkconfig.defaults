# Console: default to UART0 (the USB-UART bridge present on the Freenove,
# AIPI-Lite and Waveshare 2.06 boards). The Waveshare 1.8 has no USB-UART
# bridge, so its per-board overlay (sdkconfig.waveshare_amoled_1_8) overrides
# this with USB-Serial-JTAG. Without this explicit default, a build dir that
# was previously configured for the 1.8 keeps CONFIG_ESP_CONSOLE_USB_SERIAL_JTAG
# and the Freenove/AIPI logs silently go to the wrong port.
CONFIG_ESP_CONSOLE_UART_DEFAULT=y

# ESP Event Loop on Linux
CONFIG_ESP_EVENT_POST_FROM_ISR=n
CONFIG_ESP_EVENT_POST_FROM_IRAM_ISR=n

# TLS verification: full verification against the ESP-IDF cert bundle
# (speech.fleming.ai uses a public Let's Encrypt cert; ISRG Root X2 is in the
# bundle). Do NOT set CONFIG_ESP_TLS_INSECURE / CONFIG_ESP_TLS_SKIP_SERVER_CERT_VERIFY:
# with them enabled, esp-tls disables SNI, and speech.fleming.ai rejects
# SNI-less handshakes with a fatal alert (mbedtls -0x7780 / TLS internal error).
CONFIG_MBEDTLS_CERTIFICATE_BUNDLE=y
CONFIG_MBEDTLS_CERTIFICATE_BUNDLE_DEFAULT_FULL=y

# Enable DTLS-SRTP
CONFIG_MBEDTLS_SSL_PROTO_DTLS=y

# libpeer requires large stack allocations
CONFIG_ESP_MAIN_TASK_STACK_SIZE=16384

# Defaults to partitions.csv
CONFIG_PARTITION_TABLE_CUSTOM=y

# Set highest CPU Freq
CONFIG_ESP_DEFAULT_CPU_FREQ_MHZ_240=y

CONFIG_SPIRAM=y
CONFIG_SPIRAM_MODE_OCT=y

# Disable Watchdog
# CONFIG_ESP_INT_WDT is not set
# CONFIG_ESP_TASK_WDT_EN is not set

# Enable Compiler Optimization
CONFIG_COMPILER_OPTIMIZATION_PERF=y
CONFIG_COMPILER_OPTIMIZATION_ASSERTIONS_DISABLE=y

# ESP_HTTP_SERVER
CONFIG_HTTPD_MAX_REQ_HDR_LEN=2048
CONFIG_HTTPD_MAX_URI_LEN=1024

# LVGL
CONFIG_LV_FONT_MONTSERRAT_20=y
# 32 px font for the big "Listening" / "Paused" mode indicator
CONFIG_LV_FONT_MONTSERRAT_32=y
# Small fonts for the AIPI-Lite's 128x128 display (status 16, messages 12)
CONFIG_LV_FONT_MONTSERRAT_12=y
CONFIG_LV_FONT_MONTSERRAT_16=y

CONFIG_ESPTOOLPY_FLASHSIZE_16MB=y
CONFIG_ESPTOOLPY_FLASHSIZE="16MB"
